# Copy this file to secrets.toml (same folder) and paste your key once.
# secrets.toml is gitignored — do not commit it.
#
#   copy secrets.toml.example secrets.toml
#
# Then edit secrets.toml:

ALPHA_VANTAGE_API_KEY = "paste_your_key_here"
